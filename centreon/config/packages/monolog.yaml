# Legacy monolog configuration — reuses the platform-wide MON-151077 layout
# declared under config.new/. Keeping a single source of truth avoids the
# legacy / new kernels drifting on channels, paths or formatters.
imports:
    - { resource: '../../config.new/packages/monolog.yaml' }
