doctrine:
    dbal:
        default_connection:       default
        connections:

            default:
                dbname:           '%database_db%'
                user:             '%database_user%'
                password:         '%database_password%'
                host:             '%database_host%'
                mapping_types:
                  enum: string

            centreon_storage:
                dbname:           '%database_dbstg%'
                user:             '%database_user%'
                password:         '%database_password%'
                host:             '%database_host%'
                mapping_types:
                  enum: string
                # driver: 'pdo_mysql'
                # server_version: 'mariadb-10.11'

        # IMPORTANT: You MUST configure your server version,
        # either here or in the DATABASE_URL env var (see .env file)
        #server_version: '13'
