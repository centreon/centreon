doctrine:
    dbal:
        default_connection:       default
        connections:
            default:
                dbname:           '%database_db%'
                user:             '%database_user%'
                password:         '%database_password%'
                host:             '%database_host%'
                mapping_types:
                  enum: string
                # driver: 'pdo_mysql'
                # server_version: 'mariadb-10.11'
            centreon_storage:
                dbname:           '%database_dbstg%'
                user:             '%database_user%'
                password:         '%database_password%'
                host:             '%database_host%'
                mapping_types:
                  enum: string
                # driver: 'pdo_mysql'
                # server_version: 'mariadb-10.11
        # connections:
        #     default:
        #         url: 'mysql://%database_user%:%database_password%@%database_host%:%database_port%/%database_db%?serverVersion=5.7'
        #         driver: 'pdo_mysql'
        #         server_version: '5.7'
        #         charset: 'utf8'
                # wrapper_class: App\Domain\Doctrine\DynamicConnection

        # IMPORTANT: You MUST configure your server version,
        # either here or in the DATABASE_URL env var (see .env file)
        #server_version: '13'
    # orm:
    #     auto_generate_proxy_classes: true
    #     naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
    #     auto_mapping: true
