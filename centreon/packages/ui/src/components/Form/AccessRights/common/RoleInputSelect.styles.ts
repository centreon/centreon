import { makeStyles } from 'tss-react/mui';

const useStyles = makeStyles()(() => ({
  roleInputSelect: {
    flexShrink: 0,
    lineHeight: '1.5',
    minWidth: '6rem'
  }
}));

export { useStyles };
