{
  "name": "@centreon/ui-context",
  "version": "23.4.6-MON-build-npm-packages.31",
  "description": "Centreon UI shared context",
  "main": "dist/index.js",
  "keywords": [],
  "author": "centreon@centreon.com",
  "license": "GPL-2.0",
  "scripts": {
    "eslint": "eslint ./src --ext .ts",
    "eslint:fix": "pnpm run eslint --fix",
    "build:swc:only": "swc src --out-dir dist --sync --config-file ../js-config/swc/base.json",
    "build:lib": "pnpm build:swc:only && tsc",
    "build:lib:watch": "pnpm build:swc:only -w & tsc -w"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.59",
    "@swc/core": "^1.3.24"
  }
}
