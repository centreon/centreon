meta {
  name: GET /connectors - List connectors - HTTP 200
  type: http
  seq: 3
}

get {
  url: {{baseUrlWeb}}/configuration/connectors
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Editor can list connectors.", function () {
    const body = res.getBody();
    expect(body).to.be.an("object");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
