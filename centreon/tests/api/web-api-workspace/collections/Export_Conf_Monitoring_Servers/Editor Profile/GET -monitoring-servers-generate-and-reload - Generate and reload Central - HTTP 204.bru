meta {
  name: GET /monitoring-servers/1/generate-and-reload - Generate and reload Central - HTTP 204
  type: http
  seq: 6
}

get {
  url: {{baseUrlWeb}}/configuration/monitoring-servers/1/generate-and-reload
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

tests {
  test("Editor can generate and reload Central configuration.", function () {
    expect(res.status).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
