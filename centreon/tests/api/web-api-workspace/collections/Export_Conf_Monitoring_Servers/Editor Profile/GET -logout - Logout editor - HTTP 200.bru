meta {
  name: GET /logout - Logout editor - HTTP 200
  type: http
  seq: 10
}

get {
  url: {{baseUrlWeb}}/logout
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Logout successful.", function () {
    const body = res.getBody();
    expect(body).to.be.an("object");
    bru.deleteVar("currentUserName");
    bru.deleteVar("currentUserID");
    bru.deleteEnvVar("authToken");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
