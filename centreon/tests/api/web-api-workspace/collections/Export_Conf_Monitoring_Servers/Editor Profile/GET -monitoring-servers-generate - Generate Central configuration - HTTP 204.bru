meta {
  name: GET /monitoring-servers/1/generate - Generate Central configuration - HTTP 204
  type: http
  seq: 4
}

get {
  url: {{baseUrlWeb}}/configuration/monitoring-servers/1/generate
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

tests {
  test("Editor can generate Central configuration.", function () {
    expect(res.status).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
