meta {
  name: GET /monitoring-servers/reload - Reload all configurations - HTTP 204
  type: http
  seq: 8
}

get {
  url: {{baseUrlWeb}}/configuration/monitoring-servers/reload
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

tests {
  test("Editor can reload all monitoring servers configurations.", function () {
    expect(res.status).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
