meta {
  name: GET /monitoring-servers/generate - Generate all configurations - HTTP 204
  type: http
  seq: 6
}

get {
  url: {{baseUrlWeb}}/configuration/monitoring-servers/generate
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

tests {
  test("Administrator can generate all monitoring servers configurations.", function () {
    expect(res.status).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
