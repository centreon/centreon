meta {
  name: GET /monitoring/resources/hosts - List resources by service type - HTTP 200
  type: http
  seq: 3
}

get {
  url: {{baseUrlWeb}}/monitoring/resources/hosts?types=["service"]
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Administrator profile can list monitoring resources by parent (service type).", function () {
    const body = res.getBody();
    expect(body).to.have.property("result").that.is.an("array");
    expect(body).to.have.property("meta").that.is.an("object");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
