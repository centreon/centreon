meta {
  name: GET /monitoring/resources/hosts - List resources forbidden - HTTP 403
  type: http
  seq: 2
}

get {
  url: {{baseUrlWeb}}/monitoring/resources/hosts?types=["host"]
  body: none
  auth: inherit
}

assert {
  res.status: eq 403
}

settings {
  encodeUrl: true
  timeout: 0
}
