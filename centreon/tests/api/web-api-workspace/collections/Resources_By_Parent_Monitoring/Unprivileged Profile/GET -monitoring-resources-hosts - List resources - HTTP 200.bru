meta {
  name: GET /monitoring/resources/hosts - List resources - HTTP 200
  type: http
  seq: 2
}

get {
  url: {{baseUrlWeb}}/monitoring/resources/hosts?types=["host"]
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Unprivileged user gets empty resources list (no ACL access).", function () {
    const body = res.getBody();
    expect(body).to.have.property("result").that.is.an("array");
    expect(body.result).to.have.lengthOf(0);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
