meta {
  name: PUT /authentication/users/{alias}/password - Update operator password - HTTP 204
  type: http
  seq: 2
}

put {
  url: {{baseUrlWeb}}/authentication/users/{{user_operator_login}}/password
  body: json
  auth: inherit
}

body:json {
  {
    "old_password": "{{user_operator_password}}",
    "new_password": "{{operatorNewPassword}}"
  }
}

assert {
  res.status: eq 204
}

tests {
  test("Operator can update its own password.", function () {
    expect(res.getBody()).to.be.empty;
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
