meta {
  name: PUT /authentication/users/{alias}/password - Update unprivileged password - HTTP 204
  type: http
  seq: 2
}

put {
  url: {{baseUrlWeb}}/authentication/users/{{user_unprivileged_login}}/password
  body: json
  auth: inherit
}

body:json {
  {
    "old_password": "{{user_unprivileged_password}}",
    "new_password": "{{unprivilegedNewPassword}}"
  }
}

assert {
  res.status: eq 204
}

tests {
  test("Unprivileged user can update its own password.", function () {
    expect(res.getBody()).to.be.empty;
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
