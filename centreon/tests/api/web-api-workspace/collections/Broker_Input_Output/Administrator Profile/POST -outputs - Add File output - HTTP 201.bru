meta {
  name: POST /outputs - Add File output - HTTP 201
  type: http
  seq: 4
}

post {
  url: {{baseUrlWeb}}/configuration/broker/1/outputs
  body: json
  auth: inherit
}

body:json {
  {
    "name": "output-test-3",
    "type": 11,
    "parameters": {
      "path": "/some/random/path/",
      "protocol": "bbdo",
      "max_size": 512,
      "filters_category": ["correlation"]
    }
  }
}

assert {
  res.status: eq 201
}

tests {
  test("File output created successfully.", function () {
    const body = res.getBody();
    expect(body).to.have.property("id").that.is.a("number");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
