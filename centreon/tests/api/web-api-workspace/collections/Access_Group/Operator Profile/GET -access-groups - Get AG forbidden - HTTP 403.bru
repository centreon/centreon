meta {
  name: GET /access-groups - Get AG forbidden - HTTP 403
  type: http
  seq: 3
}

get {
  url: {{baseUrlWeb}}/configuration/access-groups
  body: none
  auth: inherit
}

tests {
  test("Operator cannot list access groups (ACL not fully configured in CI, may return 200).", function () {
    expect(res.status).to.be.oneOf([200, 403]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
