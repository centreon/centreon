meta {
  name: GET /contacts/groups - List contact groups - HTTP 200
  type: http
  seq: 3
}

get {
  url: {{baseUrlWeb}}/configuration/contacts/groups
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Editor can list contact groups.", function () {
    const body = res.getBody();
    expect(body).to.have.property("result").that.is.an("array");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
