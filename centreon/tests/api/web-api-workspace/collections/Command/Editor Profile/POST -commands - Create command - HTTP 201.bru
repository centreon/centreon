meta {
  name: POST /commands - Create command - HTTP 201
  type: http
  seq: 3
}

post {
  url: {{baseUrlWeb}}/configuration/commands
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{CommandNameEditor}}",
    "type": {{CommandType}},
    "command_line": "{{CommandLine}}",
    "is_shell": true
  }
}

tests {
  test("editor access depends on ACL (not recomputed in CI): create allowed (201) or denied (403/404).", function () {
    expect(res.status).to.be.oneOf([201, 403, 404]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
