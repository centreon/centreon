meta {
  name: GET /commands - List commands - HTTP 200
  type: http
  seq: 3
}

get {
  url: {{baseUrlWeb}}/configuration/commands
  body: none
  auth: inherit
}

tests {
  test("administrator access depends on ACL (not recomputed in CI): list allowed (200) or denied (403/404).", function () {
    expect(res.status).to.be.oneOf([200, 403, 404]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
