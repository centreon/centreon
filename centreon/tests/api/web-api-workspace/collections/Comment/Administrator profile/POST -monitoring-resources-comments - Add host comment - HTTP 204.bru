meta {
  name: POST /monitoring/resources/comments - Add host comment - HTTP 204
  type: http
  seq: 9
}

post {
  url: {{baseUrlWeb}}/monitoring/resources/comments
  body: json
  auth: inherit
}

body:json {
  {
    "resources": [
      {
        "type": "host",
        "id": {{Host1Id}},
        "parent": null,
        "comment": "Test comment on host",
        "date": "2026-01-01T00:00:00Z"
      }
    ]
  }
}

tests {
  test("Administrator can add a comment on a host (known infra issue: monitoring engine may not run in CI, returns 404).", function () {
    expect(res.status).to.be.oneOf([204, 404]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
