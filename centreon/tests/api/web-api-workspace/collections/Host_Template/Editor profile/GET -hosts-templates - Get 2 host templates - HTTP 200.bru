meta {
  name: GET /hosts/templates - Get 2 host templates - HTTP 200
  type: http
  seq: 8
}

get {
  url: {{baseUrlWeb}}/configuration/hosts/templates?sort_by={"id":"DESC"}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("The host templates are listed with all of their parameters, HT3 is absent.", function () {
    const body = res.getBody();
    const ht4 = body.result.find(r => r.id === parseInt(bru.getEnvVar("HostTemplate4Id")));
    const ht3 = body.result.find(r => r.id === parseInt(bru.getEnvVar("HostTemplate3Id")));
    const ht1 = body.result.find(r => r.id === parseInt(bru.getEnvVar("HostTemplate1Id")));
    expect(ht3).to.be.undefined;
    expect(ht4).to.include({
      "id": parseInt(bru.getEnvVar("HostTemplate4Id")),
      "name": bru.getEnvVar("HostTemplate4Name"),
      "alias": bru.getEnvVar("HostTemplate4Alias"),
      "snmp_version": "1",
      "timezone_id": null,
      "severity_id": null,
      "check_timeperiod_id": null,
      "max_check_attempts": null,
      "normal_check_interval": null,
      "retry_check_interval": null,
      "note_url": null,
      "note": null,
      "action_url": null,
      "icon_id": null,
      "is_locked": false
    });
    expect(ht1).to.include({
      "id": parseInt(bru.getEnvVar("HostTemplate1Id")),
      "name": bru.getEnvVar("HostTemplate1Name"),
      "alias": bru.getEnvVar("HostTemplate1Alias"),
      "snmp_version": "2c",
      "timezone_id": 3,
      "severity_id": parseInt(bru.getEnvVar("HostSeverity1Id")),
      "check_timeperiod_id": 1,
      "max_check_attempts": 3,
      "normal_check_interval": 5,
      "retry_check_interval": 2,
      "note_url": "noteUrl-value",
      "note": "note-value",
      "action_url": "actionUrl-value",
      "icon_id": 1,
      "is_locked": false
    });
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
