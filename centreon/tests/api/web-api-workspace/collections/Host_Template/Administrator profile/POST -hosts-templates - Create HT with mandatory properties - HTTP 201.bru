meta {
  name: POST /hosts/templates - Create HT with mandatory properties - HTTP 201
  type: http
  seq: 7
}

post {
  url: {{baseUrlWeb}}/configuration/hosts/templates
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{HostTemplate2Name}}",
    "alias": "{{HostTemplate2Alias}}"
  }
}

assert {
  res.status: eq 201
}

tests {
  test("The host template has been successfully created, with name '{{HostTemplate2Name}}' and alias '{{HostTemplate2Alias}}'.", function () {
    const body = res.getBody();
    bru.setEnvVar("HostTemplate2Id", body.id);
    expect(body).to.include({
      "id": body.id,
      "name": bru.getEnvVar("HostTemplate2Name"),
      "alias": bru.getEnvVar("HostTemplate2Alias"),
      "snmp_version": null,
      "timezone_id": null,
      "severity_id": null,
      "check_timeperiod_id": null,
      "note_url": null,
      "note": null,
      "action_url": null,
      "max_check_attempts": null,
      "normal_check_interval": null,
      "retry_check_interval": null,
      "icon_id": null,
      "is_locked": false
    });
    expect(body.categories).to.be.an("array").that.is.empty;
    expect(body.templates).to.be.an("array").that.is.empty;
    expect(body.macros).to.be.an("array").that.is.empty;
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
