meta {
  name: POST /clapi - Create engine conf for poller-2 - HTTP 200
  type: http
  seq: 7
}

post {
  url: {{baseUrlWeb}}/api/index.php?action=action&object=centreon_clapi
  body: json
  auth: none
}

headers {
  centreon-auth-token: {{authToken}}
}

body:json {
  {
    "action": "add",
    "object": "ENGINECFG",
    "values": "Centreon Engine cfg for poller-2;Poller-2;Just a small comment"
  }
}

assert {
  res.status: eq 200
}

tests {
  test("Engine configuration for Poller-2 created successfully.", function () {
    const body = res.getBody();
    expect(body).to.be.an("object");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
