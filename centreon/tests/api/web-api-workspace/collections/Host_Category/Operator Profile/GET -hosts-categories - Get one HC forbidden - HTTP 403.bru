meta {
  name: GET /hosts/categories - Get one HC forbidden - HTTP 403
  type: http
  seq: 7
}

get {
  url: {{baseUrlWeb}}/configuration/hosts/categories/{{HostCategory2Id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 403
}

tests {
  test("The user does not have the correct rights to access host categories.", function () {
    const body = res.getBody();
    expect(body.message).to.match(/not allowed/i);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
