meta {
  name: DELETE /administration/vaults/configurations - Delete vault configuration - HTTP 204
  type: http
  seq: 4
}

delete {
  url: {{baseUrlWeb}}/administration/vaults/configurations
  body: none
  auth: inherit
}

tests {
  test("DELETE /administration/vaults/configurations - Delete vault configuration - HTTP 204 (returns 404 if feature unavailable in CI).", function () {
    expect(res.status).to.be.oneOf([204, 404]);
  });

  if (res.status === 204) {
    test("The vault configuration has been successfully deleted.", function () {
      expect(res.getBody()).to.be.empty;
    });

  }
}

settings {
  encodeUrl: true
  timeout: 0
}
