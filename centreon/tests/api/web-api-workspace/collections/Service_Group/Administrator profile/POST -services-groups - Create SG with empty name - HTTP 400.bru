meta {
  name: POST /services/groups - Create SG with empty name - HTTP 400
  type: http
  seq: 9
}

post {
  url: {{baseUrlWeb}}/configuration/services/groups
  body: json
  auth: inherit
}

body:json {
  {
    "name": "",
    "alias": "Hello, I'm your ghost group!"
  }
}

assert {
  res.status: eq 400
}

tests {
  test("The service group has not been created, because the name is empty.", function () {
    const body = res.getBody();
    expect(body.message).to.eql("[NewServiceGroup::name] The string is empty, but non empty string was expected");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
