meta {
  name: DELETE /services/groups - Delete service group - HTTP 204
  type: http
  seq: 14
}

delete {
  url: {{baseUrlWeb}}/configuration/services/groups/{{ServiceGroup1Id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

tests {
  test("The service group '{{ServiceGroup1Name}}' has been successfully deleted.", function () {
    expect(res.getBody()).to.be.empty;
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
