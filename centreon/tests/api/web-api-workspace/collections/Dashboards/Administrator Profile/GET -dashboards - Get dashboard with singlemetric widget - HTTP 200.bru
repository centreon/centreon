meta {
  name: GET /dashboards - Get dashboard with singlemetric widget - HTTP 200
  type: http
  seq: 6
}

get {
  url: {{baseUrlWeb}}/configuration/dashboards/{{dashboardID}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Dashboard successfully retrieved with singlemetric widget.", function () {
    const body = res.getBody();
    expect(res.status).to.equal(200);
    expect(body.name).to.equal("Dashboard-Test-API-1002");
    expect(body.own_role).to.equal("editor");
    expect(body.panels[0].name).to.equal("centreon-widget-singlemetric");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
