meta {
  name: GET /dashboards - Get dashboard with clock widget - HTTP 200
  type: http
  seq: 20
}

get {
  url: {{baseUrlWeb}}/configuration/dashboards/{{dashboardID}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

tests {
  test("Dashboard successfully retrieved with clock widget.", function () {
    const body = res.getBody();
    expect(res.status).to.equal(200);
    expect(body.name).to.equal("Dashboard-Test-API-1002");
    expect(body.panels[0].name).to.equal("centreon-widget-clock");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
