meta {
  name: POST /dashboards - Add clock widget - HTTP 204
  type: http
  seq: 19
}

post {
  url: {{baseUrlWeb}}/configuration/dashboards/{{dashboardID}}
  body: json
  auth: inherit
}

body:json {
  {
    "panels": [
      {
        "id": null,
        "name": "centreon-widget-clock",
        "widget_type": "/widgets/clock",
        "layout": { "x": 0, "y": 0, "width": 5, "height": 3, "min_width": 2, "min_height": 2 },
        "widget_settings": "{\"data\":{},\"options\":{\"displayType\":\"clock\",\"showTimezone\":true,\"showDate\":true,\"timeFormat\":\"12\"}}"
      }
    ]
  }
}

assert {
  res.status: eq 204
}

tests {
  test("Clock widget added successfully.", function () {
    expect(res.status).to.equal(204);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
