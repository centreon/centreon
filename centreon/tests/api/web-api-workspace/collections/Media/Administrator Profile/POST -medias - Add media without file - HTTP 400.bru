meta {
  name: POST /configuration/medias - Add media without file - HTTP 500 (known PHP bug, should be 400)
  type: http
  seq: 3
}

post {
  url: {{baseUrlWeb}}/configuration/medias
  body: none
  auth: inherit
}


tests {
  test("Add media without file returns a client error (intended 400, currently 500 - known PHP bug)", function () {
    expect(res.status).to.be.oneOf([400, 500]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
