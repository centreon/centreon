meta {
  name: DELETE /acc - Delete ACC forbidden - HTTP 403
  type: http
  seq: 5
}

delete {
  url: {{baseUrlWeb}}/configuration/additional-connector-configurations/{{ACC3Id}}
  body: none
  auth: inherit
}

tests {
  test("Unprivileged user cannot delete an ACC (known issue: pollers may not exist in CI, returns 404 instead of 403).", function () {
    expect(res.status).to.be.oneOf([403, 404]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
