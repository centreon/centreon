meta {
  name: GET /acc - List ACC forbidden - HTTP 403
  type: http
  seq: 4
}

get {
  url: {{baseUrlWeb}}/configuration/additional-connector-configurations
  body: none
  auth: inherit
}

tests {
  test("Operator cannot list ACC (known issue: pollers may not exist in CI, returns 404 instead of 403).", function () {
    expect(res.status).to.be.oneOf([403, 404]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
