meta {
  name: PUT /acc - Update ACC forbidden - HTTP 403
  type: http
  seq: 5
}

put {
  url: {{baseUrlWeb}}/configuration/additional-connector-configurations/{{ACC3Id}}
  body: json
  auth: inherit
}

body:json {
  {
    "name": "acc-operator-update",
    "description": null,
    "pollers": [1],
    "type": "vmware_v6",
    "parameters": {
      "port": 5780,
      "vcenters": [{"name": "my-vcenter", "url": "http://10.10.10.10/sdk", "username": "admin", "password": "my-pwd"}]
    }
  }
}

assert {
  res.status: eq 403
}

tests {
  test("Operator cannot update an ACC.", function () {
    expect(res.status).to.equal(403);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
