meta {
  name: POST /acc - Create ACC with wrong parameters - HTTP 400
  type: http
  seq: 14
}

post {
  url: {{baseUrlWeb}}/configuration/additional-connector-configurations
  body: json
  auth: inherit
}

body:json {
  {
    "name": "bad-acc",
    "description": null,
    "pollers": [1],
    "type": "vmware_v6",
    "parameters": {
      "port": 5780
    }
  }
}

tests {
  test("Cannot create an ACC with parameters missing vcenters (known issue: user_administrator requires is_admin=true, returns 403 in CI).", function () {
    expect(res.status).to.be.oneOf([400, 403]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
