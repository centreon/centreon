meta {
  name: POST /acc - Create ACC with wrong parameters - HTTP 400
  type: http
  seq: 14
}

post {
  url: {{baseUrlWeb}}/configuration/additional-connector-configurations
  body: json
  auth: inherit
}

body:json {
  {
    "name": "bad-acc",
    "description": null,
    "pollers": [1],
    "type": "vmware_v6",
    "parameters": {
      "port": 5780
    }
  }
}

tests {
  test("Cannot create an ACC with parameters missing vcenters (returns 500 if CI infrastructure is unavailable).", function () {
    expect(res.status).to.be.oneOf([400, 500]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
