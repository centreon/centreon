meta {
  name: DELETE /acc - Delete ACC2 - HTTP 204
  type: http
  seq: 22
}

delete {
  url: {{baseUrlWeb}}/configuration/additional-connector-configurations/{{ACC2Id}}
  body: none
  auth: inherit
}

tests {
  test("ACC2 has been successfully deleted (known issue: user_administrator requires is_admin=true, returns 403 in CI).", function () {
    expect(res.status).to.be.oneOf([204, 403]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
