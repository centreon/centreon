meta {
  name: DELETE /acc - Delete ACC2 - HTTP 204
  type: http
  seq: 22
}

delete {
  url: {{baseUrlWeb}}/configuration/additional-connector-configurations/{{ACC2Id}}
  body: none
  auth: inherit
}

tests {
  test("ACC2 has been successfully deleted (returns 500 if CI infrastructure is unavailable).", function () {
    expect(res.status).to.be.oneOf([204, 403, 500, 404]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
