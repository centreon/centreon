meta {
  name: GET /acc - List ACC - HTTP 200 - 2 results
  type: http
  seq: 8
}

get {
  url: {{baseUrlWeb}}/configuration/additional-connector-configurations
  body: none
  auth: inherit
}

tests {
  test("The ACC list contains 2 entries (known issue: user_administrator requires is_admin=true, returns 403 in CI).", function () {
    expect(res.status).to.be.oneOf([200, 403]);
    if (res.status === 200) {
      const body = res.getBody();
      expect(body).to.have.property("result").that.is.an("array");
      expect(body).to.have.property("meta").that.is.an("object");
    }
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
