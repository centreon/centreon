meta {
  name: POST /clapi - Create fake poller-1 - HTTP 200
  type: http
  seq: 2
}

post {
  url: {{baseUriCentreonCLAPI}}
  body: json
  auth: inherit
}

headers {
  centreon-auth-token: {{authToken}}
}

body:json {
  {
    "action": "add",
    "object": "INSTANCE",
    "values": "Poller-1;10.30.2.55;22;ZMQ;5556"
  }
}

assert {
  res.status: eq 200
}

tests {
  test("The fake poller-1 has been successfully created.", function () {
    expect(res.getBody()).to.be.an("object");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
