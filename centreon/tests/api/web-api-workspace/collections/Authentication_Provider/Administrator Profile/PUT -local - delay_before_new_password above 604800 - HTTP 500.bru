meta {
  name: PUT /local - delay_before_new_password above 604800 - HTTP 500
  type: http
  seq: 12
}

put {
  url: {{baseUrlWeb}}/administration/authentication/providers/local
  body: json
  auth: inherit
}

body:json {
  {
    "password_security_policy": {
      "password_min_length": 12,
      "has_uppercase": false,
      "has_lowercase": false,
      "has_number": true,
      "has_special_character": false,
      "attempts": 5,
      "blocking_duration": 900,
      "password_expiration": {
        "expiration_delay": 604800,
        "excluded_users": ["admin"]
      },
      "can_reuse_passwords": true,
      "delay_before_new_password": 604801
    }
  }
}

tests {
  test("Validation rejected (known issue: user_administrator requires is_admin=true, returns 403 in CI).", function () {
    expect(res.status).to.be.oneOf([500, 403]);
    if (res.status === 500) {
      const body = res.getBody();
      expect(body).to.have.property("message").that.includes("delay_before_new_password");
      expect(body).to.have.property("message").that.includes("maximum value of 604800");
    }
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
