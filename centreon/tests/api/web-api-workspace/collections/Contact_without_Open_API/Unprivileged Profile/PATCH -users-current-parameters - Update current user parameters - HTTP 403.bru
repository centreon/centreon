meta {
  name: PATCH /users/current/parameters - Update current user parameters - HTTP 403
  type: http
  seq: 3
}

patch {
  url: {{baseUrlWeb}}/configuration/users/current/parameters
  body: json
  auth: inherit
}

body:json {
  {
    "theme": "dark"
  }
}

assert {
  res.status: eq 403
}

tests {
  test("Unprivileged user cannot update current user parameters.", function () {
    expect(res.status).to.equal(403);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
