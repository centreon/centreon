meta {
  name: POST /services/templates - Create ST0 empty - HTTP 201
  type: http
  seq: 8
}

post {
  url: {{baseUrlWeb}}/configuration/services/templates
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{ServiceTemplate0Name}}",
    "alias": "{{ServiceTemplate0Alias}}"
  }
}

assert {
  res.status: eq 201
}

tests {
  test("The service template ST0 has been successfully created with only mandatory properties.", function () {
    const body = res.getBody();
    bru.setEnvVar("ServiceTemplate0Id", body.id);
    expect(body).to.include({
      "id": body.id,
      "name": bru.getEnvVar("ServiceTemplate0Name"),
      "alias": bru.getEnvVar("ServiceTemplate0Alias"),
      "service_template_id": null,
      "check_timeperiod_id": null,
      "max_check_attempts": null,
      "normal_check_interval": null,
      "retry_check_interval": null,
      "note": null,
      "note_url": null,
      "action_url": null,
      "icon_id": null,
      "severity_id": null,
      "is_locked": false
    });
    expect(body.host_templates).to.be.an("array").that.is.empty;
    expect(body.macros).to.be.an("array").that.is.empty;
    expect(body.categories).to.be.an("array").that.is.empty;
    expect(body.groups).to.be.an("array").that.is.empty;
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
