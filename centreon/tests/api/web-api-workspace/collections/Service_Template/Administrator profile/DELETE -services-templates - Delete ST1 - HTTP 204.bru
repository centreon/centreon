meta {
  name: DELETE /services/templates - Delete ST1 - HTTP 204
  type: http
  seq: 22
}

delete {
  url: {{baseUrlWeb}}/configuration/services/templates/{{ServiceTemplate1Id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

tests {
  test("The service template ST1 has been successfully deleted.", function () {
    expect(res.getBody()).to.be.empty;
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
