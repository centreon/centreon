meta {
  name: POST /services - Create service without template - HTTP 400
  type: http
  seq: 18
}

post {
  url: {{baseUrlWeb}}/configuration/services
  body: json
  auth: inherit
}

body:json {
  {
    "name": "service 333",
    "host_id": {{Host1Id}}
  }
}

assert {
  res.status: eq 400
}

tests {
  test("The service has not been created because the template is missing.", function () {
    const body = res.getBody();
    expect(body.message).to.equal("[service_template_id] The property service_template_id is required\n");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
