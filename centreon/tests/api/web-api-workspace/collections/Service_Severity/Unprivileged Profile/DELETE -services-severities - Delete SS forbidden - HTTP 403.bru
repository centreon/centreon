meta {
  name: DELETE /services/severities - Delete SS forbidden - HTTP 403
  type: http
  seq: 5
}

delete {
  url: {{baseUrlWeb}}/configuration/services/severities/{{ServiceSeverity2Id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 403
}

tests {
  test("The user does not have the correct rights to delete a service severity.", function () {
    const body = res.getBody();
    expect(body.message).to.match(/not allowed/i);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
