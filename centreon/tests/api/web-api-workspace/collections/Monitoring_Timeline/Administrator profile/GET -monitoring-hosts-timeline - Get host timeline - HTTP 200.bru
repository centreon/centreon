meta {
  name: GET /monitoring/hosts/{id}/timeline - Get host timeline - HTTP 200
  type: http
  seq: 2
}

get {
  url: {{baseUrlWeb}}/monitoring/hosts/{{Host1Id}}/timeline
  body: none
  auth: inherit
}

tests {
  test("administrator access depends on ACL (not recomputed in CI): allowed or denied (403/404 when the resource is ACL-filtered).", function () {
    expect(res.status).to.be.oneOf([200, 403, 404]);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
