meta {
  name: DELETE /notifications - Delete notification1 - HTTP 204
  type: http
  seq: 9
}

delete {
  url: {{baseUrlWeb}}/configuration/notifications/{{Notification1Id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 204
}

tests {
  test("Notification1 deleted.", function () {
    expect(res.getBody()).to.be.empty;
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
