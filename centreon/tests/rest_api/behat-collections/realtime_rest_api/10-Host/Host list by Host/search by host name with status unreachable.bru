meta {
  name: search by host name with status unreachable
  type: http
  seq: 5
}

get {
  url: http://{{url}}/centreon/api/index.php?action=list&object=centreon_realtime_hosts&search={{host_name}}&status=unreachable
  body: none
  auth: none
}

query {
  action: list
  object: centreon_realtime_hosts
  search: {{host_name}}
  status: unreachable
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

tests {
  var helpers = eval(globals.loadHelpers);
  helpers.test200();
  helpers.testJson();
}
