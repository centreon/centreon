meta {
  name: search by host name with status up
  type: http
  seq: 2
}

get {
  url: http://{{url}}/centreon/api/index.php?action=list&object=centreon_realtime_hosts&search={{host_name}}&status=up
  body: none
  auth: none
}

query {
  action: list
  object: centreon_realtime_hosts
  search: {{host_name}}
  status: up
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

tests {
  var helpers = eval(globals.loadHelpers);
  helpers.test200();
  helpers.testJson();
}
