meta {
  name: search by host name
  type: http
  seq: 1
}

get {
  url: http://{{url}}/centreon/api/index.php?action=list&object=centreon_realtime_hosts&search={{host_name}}
  body: none
  auth: none
}

query {
  action: list
  object: centreon_realtime_hosts
  search: {{host_name}}
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

assert {
  res.body: isJson
}

tests {
  const { checkHostList } = require('./lib/helpers');
  
  checkHostList();
  
}
