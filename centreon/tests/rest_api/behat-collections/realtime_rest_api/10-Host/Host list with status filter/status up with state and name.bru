meta {
  name: status up with state and name
  type: http
  seq: 1
}

get {
  url: http://{{url}}/centreon/api/index.php?action=list&object=centreon_realtime_hosts&status=up&fields=name, state
  body: none
  auth: none
}

query {
  action: list
  object: centreon_realtime_hosts
  status: up
  fields: name, state
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

tests {
  var helpers = eval(globals.loadHelpers);
  helpers.test200();
  helpers.testJson();
}
