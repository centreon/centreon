meta {
  name: list with viewType
  type: http
  seq: 1
}

get {
  url: http://{{url}}/centreon/api/index.php?action=list&object=centreon_realtime_hosts&viewType=all,unhandled,problems
  body: none
  auth: none
}

query {
  action: list
  object: centreon_realtime_hosts
  viewType: all,unhandled,problems
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

tests {
  var helpers = eval(globals.loadHelpers);
  helpers.checkHostList();
}
