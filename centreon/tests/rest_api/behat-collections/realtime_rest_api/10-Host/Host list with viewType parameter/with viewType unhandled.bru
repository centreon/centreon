meta {
  name: with viewType unhandled
  type: http
  seq: 3
}

get {
  url: http://{{url}}/centreon/api/index.php?action=list&object=centreon_realtime_hosts&viewType=unhandled
  body: none
  auth: none
}

query {
  action: list
  object: centreon_realtime_hosts
  viewType: unhandled
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

tests {
  var helpers = eval(globals.loadHelpers);
  helpers.test200();
  helpers.testJson();
}
