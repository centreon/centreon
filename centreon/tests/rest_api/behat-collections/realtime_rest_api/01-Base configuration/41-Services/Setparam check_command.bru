meta {
  name: Setparam check_command
  type: http
  seq: 10
}

post {
  url: http://{{url}}/centreon/api/index.php?action=action&object=centreon_clapi
  body: json
  auth: none
}

query {
  action: action
  object: centreon_clapi
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

body:json {
  {
    "action": "setparam",
    "object": "service",
    "values": "ack_host;ack_service;check_command;{{command_name}}"
  }
}

tests {
  tests["Status code is 200"] = res.getStatus() === 200;
}
