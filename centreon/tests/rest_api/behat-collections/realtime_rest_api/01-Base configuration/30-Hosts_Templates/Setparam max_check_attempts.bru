meta {
  name: Setparam max_check_attempts
  type: http
  seq: 4
}

post {
  url: http://{{url}}/centreon/api/index.php?action=action&object=centreon_clapi
  body: json
  auth: none
}

query {
  action: action
  object: centreon_clapi
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

body:json {
  {
    "action":"setparam",
    "object":"htpl",
    "values": "{{htpl_name}};max_check_attempts;{{host_max_check_attempts}}"
  }
}

tests {
  test("Status code is 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
