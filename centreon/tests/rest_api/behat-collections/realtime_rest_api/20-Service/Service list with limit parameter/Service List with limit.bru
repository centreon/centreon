meta {
  name: Service List with limit
  type: http
  seq: 1
}

get {
  url: http://{{url}}/centreon/api/index.php?action=list&object=centreon_realtime_services&limit={{my_limit}}
  body: none
  auth: none
}

query {
  action: list
  object: centreon_realtime_services
  limit: {{my_limit}}
}

headers {
  Content-Type: application/json
  centreon-auth-token: {{token}}
}

tests {
  var helpers = eval(globals.loadHelpers);
  helpers.checkServiceList();
}
