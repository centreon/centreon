type: object
properties:
  host_templates:
    type: array
    descriptions: "IDs of host templates linked to this service template."
    items:
      type: integer
      description: "Host template ID"
    example: [ 1, 2 ]
