type: object
required:
  - id
  - role

properties:

  id:
    type: integer
    description: "Contact group ID"
    example: 1

  role:
    type: string
    enum: [ viewer, editor ]
    description: "The role of the contact group for this dashboard."
    example: "viewer"
