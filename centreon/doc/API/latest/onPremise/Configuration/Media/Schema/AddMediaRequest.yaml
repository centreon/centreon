type: object
required: ['directory', 'data']
properties:
  directory:
    type: string
    description: "Directory where the file will be created"
  data:
    type: string
    format: binary
    description: "File content"
