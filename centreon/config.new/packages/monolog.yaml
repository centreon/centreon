monolog:
  channels: ['deprecation']
  handlers:
    deprecation:
      type: stream
      path: '%kernel.logs_dir%/%kernel.environment%.deprecations.log'
      max_files: 2
      channels: ['deprecation']
      file_permission: 0660
