api_platform:
    resource: .
    type: api_platform
    prefix: '/api'

# Backward-compat alias: only already-migrated resources are duplicated under /api/latest, see LegacyApiPrefixAliasLoader.
api_platform_legacy:
    resource: 'App\Shared\Infrastructure\ApiPlatform\Routing\LegacyApiPrefixAliasLoader'
    type: service
