{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "Add a contact group access right of a dashboard",
    "type": "object",
    "required": [
        "id",
        "role"
    ],
    "additionalProperties": false,
    "properties": {
        "id": {
            "type": "integer"
        },
        "role": {
            "type": "string",
            "enum": [
                "viewer",
                "editor"
            ]
        }
    }
}
