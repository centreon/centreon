import initialize from './initialize';

export default (resourceType) => {
  describe('Actions', () => {
    beforeEach(() => initialize({}));
    describe('delete', () => {});
    describe('duplicate', () => {});
    describe('Enable/disble', () => {});
  });
};
