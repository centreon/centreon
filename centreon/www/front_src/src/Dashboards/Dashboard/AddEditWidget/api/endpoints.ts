// import { baseEndpoint } from '../../../api/endpoints';

const baseEndpoint = `http://localhost:5005/centreon/api/latest`;

export const metricsEndpoint = `${baseEndpoint}/monitoring/dashboard/metrics/performances`;
