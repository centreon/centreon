import { makeStyles } from 'tss-react/mui';

export const useNoDataFoundStyles = makeStyles()({
  noDataFound: {
    alignItems: 'center',
    display: 'flex',
    height: '100%',
    justifyContent: 'center'
  }
});
