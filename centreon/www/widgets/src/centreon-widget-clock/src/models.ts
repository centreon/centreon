export interface PanelOptions {
  backgroundColor?: string;
  countdown?: number;
  displayType: 'clock' | 'timer';
  locale?: string;
  showDate: boolean;
  showTimezone: boolean;
  timezone?: string;
}
