---
name: Create centreon widget global health version

on:
  pull_request_target:
    types:
      - closed
    branches:
      - "master"
      - "*.x"
      - "!dev*"
    paths:
      - ".github/actions/release/action.yml"
      - ".github/workflows/release-widget-global-health.yml"
      - "widgets/centreon-widget-global-health/**"
  workflow_dispatch:

env:
  module: "widget-global-health"

jobs:
  release:
    runs-on: ubuntu-22.04
    if: github.event.pull_request.merged == true
    steps:
      - name: Checkout sources
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Release
        id: release
        uses: ./.github/actions/release
        with:
          module_name: ${{ env.module }}
          jira_token: ${{ secrets.JIRA_TOKEN }}
