name: widget-hostgroup-monitoring

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/widget-hostgroup-monitoring.yml"
      - "widgets/centreon-widget-hostgroup-monitoring/**"
  push:
    branches:
      - dev-21.10.x
    paths:
      - ".github/workflows/centreon-widget-hostgroup-monitoring.yml"
      - "widgets/centreon-widget-hostgroup-monitoring/**"
    tags:
      - centreon-widget-hostgroup-monitoring-22.04.**

env:
  version: "21.10"

jobs:
  package:
    uses: ./.github/workflows/widget-package.yml
    with:
      widget_name: hostgroup-monitoring
    secrets:
      nexus_username: ${{ secrets.ARTI_USER }}
      nexus_password: ${{ secrets.ARTI_PASSWD }}

  delivery:
    needs: [package]
    uses: ./.github/workflows/widget-delivery.yml
    with:
      widget_name: hostgroup-monitoring
    secrets:
      nexus_username: ${{ secrets.ARTI_USER }}
      nexus_password: ${{ secrets.ARTI_PASSWD }}
