name: dsm

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

on:
  workflow_dispatch:
    inputs:
      labels:
        description: "Labels to add to the run (comma separated)"
        required: false
        default: ""
        type: string
  pull_request:
    paths:
      - ".github/workflows/dsm.yml"
      - ".version"
      - ".version.centreon-dsm"
      - "centreon-dsm/**"
  push:
    branches:
      - develop
      - dev-[2-9][0-9].[0-9][0-9].x
      - master
      - "[2-9][0-9].[0-9][0-9].x"
      - 'release-[2-9][0-9].[0-9][0-9]-next'
      - 'hotfix-[2-9][0-9].[0-9][0-9]-next'
    paths:
      - ".version"
      - ".version.centreon-dsm"
      - "centreon-dsm/**"
    tags:
      - "centreon-dsm-*"

env:
  module: dsm

jobs:
  dependency-scan:
    uses: centreon/security-tools/.github/workflows/dependency-analysis.yml@main

  get-environment:
    needs: [dependency-scan]
    if: github.event.pull_request.head.repo.fork != true
    uses: ./.github/workflows/get-environment.yml
    with:
      version_file: centreon-dsm/www/modules/centreon-dsm/conf.php
      labels: ${{ inputs.labels || '' }}

  check-version-consistency:
    runs-on: ubuntu-24.04
    needs: [get-environment]
    if: |
      needs.get-environment.outputs.skip_workflow == 'false' &&
      needs.get-environment.outputs.stability == 'testing' &&
      github.event.pull_request.head.repo.fork != true
    env:
      version: ${{ needs.get-environment.outputs.major_version }}.${{ needs.get-environment.outputs.minor_version }}
      version_file: .version.centreon-dsm
      configuration_file: centreon-dsm/www/modules/centreon-dsm/conf.php
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Check ${{ env.version_file }}
        uses: ./.github/actions/check-version-consistency
        with:
          file: ${{ env.version_file }}
          version: ${{ env.version }}

      - name: Check ${{ env.configuration_file }}
        uses: ./.github/actions/check-version-consistency
        with:
          file: ${{ env.configuration_file }}
          version: ${{ env.version }}

  changes:
    if: |
      needs.get-environment.outputs.skip_workflow == 'false' &&
      needs.get-environment.outputs.stability != 'stable' &&
      github.event.pull_request.head.repo.fork != true
    needs: [get-environment]
    uses: ./.github/workflows/get-changes-triggers.yml
    with:
      backend_production_paths: |
        ".version"
        ".version.centreon-dsm"
        "centreon-dsm/bin/**"
        "centreon-dsm/cron/**"
        "centreon-dsm/libinstall/**"
        "centreon-dsm/packaging/**"
        "centreon-dsm/www/modules/centreon-dsm/**"
        "!centreon-dsm/www/modules/centreon-dsm/react/**"
      frontend_production_paths: |
        "centreon-dsm/www/modules/centreon-dsm/react/**"
        "!centreon-dsm/**/cypress/**"
        "!centreon-dsm/**/*.cypress.*"
        "!centreon-dsm/**/__mocks__/**"
      backend_test_paths: |
        "centreon-dsm/.php-cs-fixer*"
        "centreon-dsm/composer.*"
        "centreon-dsm/phpstan.*"
        "centreon-dsm/rector.*"
        "centreon-dsm/tests/php/**"
      is_nightly: ${{ needs.get-environment.outputs.is_nightly }}
      base_sha: ${{ needs.get-environment.outputs.previous_release_bundle_tag }}
      labels: ${{ needs.get-environment.outputs.labels }}

  backend-lint:
    runs-on: ubuntu-24.04
    needs: [get-environment, changes]
    if: |
      needs.changes.outputs.trigger_backend_testing == 'true' &&
      needs.get-environment.outputs.skip_workflow == 'false' &&
      needs.get-environment.outputs.stability != 'stable' &&
      github.event.pull_request.head.repo.fork != true

    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup PHP
        uses: shivammathur/setup-php@accd6127cb78bee3e8082180cb391013d204ef9f # v2.37.0
        with:
          php-version: ${{ needs.get-environment.outputs.php_version }}
          coverage: none
        env:
          runner: ubuntu-24.04

      - name: Install dependencies for PHP quality tools
        run: composer install --optimize-autoloader --prefer-dist --no-interaction --no-progress --quiet --no-scripts
        working-directory: php-tools
        shell: bash

      - name: Install dependencies for centreon-dsm
        run: composer install --optimize-autoloader --prefer-dist --no-interaction --no-progress --quiet --no-scripts
        working-directory: centreon-dsm
        shell: bash

      - name: Get changed PHP files
        id: changed-php
        uses: tj-actions/changed-files@9426d40962ed5378910ee2e21d5f8c6fcbf2dd96 # v47.0.6
        with:
          use_rest_api: ${{ github.event_name == 'pull_request' }}
          files: |
            **/*.php

      - name: 🛠️ Rector checks
        if: steps.changed-php.outputs.any_changed == 'true'
        working-directory: centreon-dsm
        env:
          PHP_DIFF_FILES: ${{ steps.changed-php.outputs.all_changed_files }}
        run: |
          echo "Run of these Rector tests: rector:legacy on diff" && set -e
          composer rector:diff

      - name: 🧹 PHP-CS-Fixer checks
        if: steps.changed-php.outputs.any_changed == 'true'
        working-directory: centreon-dsm
        env:
          PHP_DIFF_FILES: ${{ steps.changed-php.outputs.all_changed_files }}
        run: |
          echo "Run of these PHP-CS-Fixer tests: cs:legacy on diff" && set -e
          composer cs:diff

      - name: Restore PHPStan result cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: centreon-dsm/.phpstan-cache
          # Unique per run (sha) so the evolving result cache is re-saved every run;
          # head_ref is the real branch name on pull_request (ref_name would be "N/merge").
          key: phpstan-cache-dsm-${{ github.head_ref || github.ref_name }}-${{ github.sha }}
          restore-keys: |
            phpstan-cache-dsm-${{ github.head_ref || github.ref_name }}-
            phpstan-cache-dsm-

      - name: 🔍 PHPStan checks
        working-directory: centreon-dsm
        run: |
          echo "Run of these PHPStan tests: stan:legacy"

          exit_code=0

          run_task() {
            task=$1                  # e.g. legacy
            cache_dir=${task//:/-}
            # PHPStan has no --tmp-dir flag; its tmp dir defaults to sys_get_temp_dir()/phpstan
            # and sys_get_temp_dir() honours $TMPDIR. Point each task at its own dir so its
            # result cache (resultCache.php) is isolated and persisted across runs.
            task_tmp="${PWD}/.phpstan-cache/${cache_dir}"
            mkdir -p "${task_tmp}"
            if TMPDIR="${task_tmp}" composer "stan:${task}" -- --no-progress; then
              echo "✅ PHPStan ${task}: Success"
            else
              echo "❌ PHPStan ${task}: Failed"
              exit_code=1
            fi
          }

          run_task legacy

          exit $exit_code

  package:
    needs: [get-environment, changes, check-version-consistency, backend-lint]
    if: |
      needs.changes.outputs.trigger_packaging == 'true' &&
      needs.get-environment.outputs.skip_workflow == 'false' &&
      needs.get-environment.outputs.stability != 'stable' &&
      ! cancelled() &&
      ! contains(needs.*.result, 'failure') &&
      ! contains(needs.*.result, 'cancelled') &&
      github.event.pull_request.head.repo.fork != true

    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.get-environment.outputs.package_matrix) }}

    runs-on: ubuntu-24.04

    container:
      image: ghcr.io/centreon/delivery-tooling/packaging:${{ needs.get-environment.outputs.major_version }}-${{ matrix.operating_system }}

    name: package ${{ matrix.distrib }}

    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Change macro
        run: |
          MACRO_REPLACEMENT_FILE="centreon-dsm/packaging/src/centreon-macroreplacement.txt"
          if [[ "${{ matrix.package_extension }}" == "rpm" ]]; then
            APACHE_USER="apache"
          else
            APACHE_USER="www-data"
          fi
          echo "s#@WEB_USER@#$APACHE_USER#g" >> $MACRO_REPLACEMENT_FILE
          find ./centreon-dsm -type f | xargs sed -i -f $MACRO_REPLACEMENT_FILE
        shell: bash

      - name: Package
        uses: ./.github/actions/package-nfpm
        with:
          nfpm_file_pattern: "centreon-dsm/packaging/*.yaml"
          module_name: centreon-dsm
          distrib: ${{ matrix.distrib }}
          package_extension: ${{ matrix.package_extension }}
          major_version: ${{ needs.get-environment.outputs.major_version }}
          minor_version: ${{ needs.get-environment.outputs.minor_version }}
          release: ${{ needs.get-environment.outputs.release }}
          arch: all
          commit_hash: ${{ github.sha }}
          cache_key: ${{ github.sha }}-${{ github.run_id }}-${{ matrix.package_extension }}-${{ matrix.distrib }}
          rpm_gpg_key: ${{ secrets.RPM_GPG_SIGNING_KEY }}
          rpm_gpg_signing_key_id: ${{ secrets.RPM_GPG_SIGNING_KEY_ID }}
          rpm_gpg_signing_passphrase: ${{ secrets.RPM_GPG_SIGNING_PASSPHRASE }}
          stability: ${{ needs.get-environment.outputs.stability }}
          php_version: ${{ needs.get-environment.outputs.php_version }}
          upload_artifacts: ${{ needs.get-environment.outputs.is_nightly == 'true' || contains(needs.get-environment.outputs.labels, 'upload-artifacts') && 'true' || 'false' }}

  deliver-sources:
    runs-on: centreon-common
    needs: [get-environment, package]
    if: |
      needs.get-environment.outputs.skip_workflow == 'false' &&
      needs.get-environment.outputs.stability == 'stable' &&
      github.event_name != 'workflow_dispatch' &&
      ! cancelled() &&
      ! contains(needs.*.result, 'failure') &&
      ! contains(needs.*.result, 'cancelled') &&
      github.event.pull_request.head.repo.fork != true

    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Deliver sources
        uses: ./.github/actions/release-sources
        with:
          bucket_directory: centreon-dsm
          module_directory: centreon-dsm
          module_name: centreon-dsm
          major_version: ${{ needs.get-environment.outputs.major_version }}
          minor_version: ${{ needs.get-environment.outputs.minor_version }}
          token_download_centreon_com: ${{ secrets.TOKEN_DOWNLOAD_CENTREON_COM }}

  deliver:
    needs: [get-environment, changes, package]
    if: |
      needs.changes.outputs.trigger_delivery == 'true' &&
      needs.get-environment.outputs.skip_workflow == 'false' &&
      contains(fromJson('["testing", "unstable"]'), needs.get-environment.outputs.stability) &&
      github.event.pull_request.head.repo.fork != true &&
      ! cancelled() &&
      ! contains(needs.*.result, 'failure') &&
      ! contains(needs.*.result, 'cancelled')
    runs-on: centreon-ubuntu-22.04

    strategy:
      matrix:
        include: ${{ fromJson(needs.get-environment.outputs.package_matrix) }}

    name: deliver ${{ matrix.distrib }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # Artifactory is delivered first and stays authoritative during the Pulp
      # migration; drop these two steps (and the deb-delivery/rpm-delivery actions)
      # once Pulp is authoritative.
      - name: Deliver RPM package on Artifactory
        if: matrix.package_extension == 'rpm'
        uses: ./.github/actions/rpm-delivery
        with:
          module_name: dsm
          distrib: ${{ matrix.distrib }}
          version: ${{ needs.get-environment.outputs.major_version }}
          artifactory_token: ${{ secrets.ARTIFACTORY_ACCESS_TOKEN }}
          cache_key: ${{ github.sha }}-${{ github.run_id }}-rpm-${{ matrix.distrib }}
          stability: ${{ needs.get-environment.outputs.stability }}
          release_type: ${{ needs.get-environment.outputs.release_type }}
          is_cloud: ${{ needs.get-environment.outputs.is_cloud }}
          delivery_type: ${{ needs.get-environment.outputs.delivery_type }}
          centreon_technique_pat: ${{ secrets.CENTREON_TECHNIQUE_PAT }}

      - name: Deliver DEB package on Artifactory
        if: matrix.package_extension == 'deb'
        uses: ./.github/actions/deb-delivery
        with:
          module_name: dsm
          distrib: ${{ matrix.distrib }}
          version: ${{ needs.get-environment.outputs.major_version }}
          artifactory_token: ${{ secrets.ARTIFACTORY_ACCESS_TOKEN }}
          cache_key: ${{ github.sha }}-${{ github.run_id }}-deb-${{ matrix.distrib }}
          stability: ${{ needs.get-environment.outputs.stability }}
          release_type: ${{ needs.get-environment.outputs.release_type }}
          is_cloud: ${{ needs.get-environment.outputs.is_cloud }}

  promote:
    needs: [get-environment, deliver]
    if: |
      needs.get-environment.outputs.skip_workflow == 'false' &&
      (contains(fromJson('["stable"]'), needs.get-environment.outputs.stability) && github.event_name != 'workflow_dispatch') &&
      ! cancelled() &&
      ! contains(needs.*.result, 'failure') &&
      ! contains(needs.*.result, 'cancelled') &&
      github.event.pull_request.head.repo.fork != true
    runs-on: centreon-ubuntu-22.04
    strategy:
      matrix:
        include: ${{ fromJson(needs.get-environment.outputs.package_matrix) }}

    name: promote ${{ matrix.distrib }}
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      # Artifactory is promoted first and stays authoritative during the Pulp
      # migration; drop this step (and the Artifactory promote-to-stable action)
      # once Pulp is authoritative.
      - name: Promote ${{ matrix.distrib }} to stable on Artifactory
        uses: ./.github/actions/promote-to-stable
        with:
          artifactory_token: ${{ secrets.ARTIFACTORY_ACCESS_TOKEN }}
          module_name: dsm
          distrib: ${{ matrix.distrib }}
          major_version: ${{ needs.get-environment.outputs.major_version }}
          minor_version: ${{ needs.get-environment.outputs.minor_version }}
          stability: ${{ needs.get-environment.outputs.stability }}
          github_ref_name: ${{ github.ref_name }}
          release_type: ${{ needs.get-environment.outputs.release_type }}
          is_cloud: ${{ needs.get-environment.outputs.is_cloud }}

  set-skip-label:
    needs: [get-environment, deliver, promote]
    if: |
      needs.get-environment.outputs.skip_workflow == 'false' &&
      ! cancelled() &&
      ! contains(needs.*.result, 'failure') &&
      ! contains(needs.*.result, 'cancelled')
    uses: ./.github/workflows/set-pull-request-skip-label.yml

  deliver-pulp:
    needs: [get-environment, changes, package]
    if: |
      needs.changes.outputs.trigger_delivery == 'true' &&
      needs.get-environment.outputs.skip_workflow == 'false' &&
      contains(fromJson('["testing", "unstable"]'), needs.get-environment.outputs.stability) &&
      github.event.pull_request.head.repo.fork != true &&
      ! cancelled() &&
      ! contains(needs.*.result, 'failure') &&
      ! contains(needs.*.result, 'cancelled')
    runs-on: centreon-ubuntu-22.04

    strategy:
      matrix:
        include: ${{ fromJson(needs.get-environment.outputs.package_matrix) }}

    name: deliver ${{ matrix.distrib }} (pulp)
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Deliver packages on Pulp
        uses: ./.github/actions/package-delivery-pulp
        continue-on-error: true
        with:
          module_name: dsm
          distrib: ${{ matrix.distrib }}
          version: ${{ needs.get-environment.outputs.major_version }}
          cache_key: ${{ github.sha }}-${{ github.run_id }}-${{ matrix.package_extension }}-${{ matrix.distrib }}
          stability: ${{ needs.get-environment.outputs.stability }}
          release_type: ${{ needs.get-environment.outputs.release_type }}
          is_cloud: ${{ needs.get-environment.outputs.is_cloud }}
          delivery_type: ${{ needs.get-environment.outputs.delivery_type }}

  promote-pulp:
    needs: [get-environment, deliver-pulp]
    if: |
      needs.get-environment.outputs.skip_workflow == 'false' &&
      (contains(fromJson('["stable"]'), needs.get-environment.outputs.stability) && github.event_name != 'workflow_dispatch') &&
      ! cancelled() &&
      ! contains(needs.*.result, 'failure') &&
      ! contains(needs.*.result, 'cancelled') &&
      github.event.pull_request.head.repo.fork != true
    runs-on: centreon-ubuntu-22.04
    strategy:
      matrix:
        include: ${{ fromJson(needs.get-environment.outputs.package_matrix) }}

    name: promote ${{ matrix.distrib }} (pulp)
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout sources
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Promote ${{ matrix.distrib }} to stable on Pulp
        uses: ./.github/actions/promote-to-stable-pulp
        continue-on-error: true
        with:
          module_name: dsm
          distrib: ${{ matrix.distrib }}
          major_version: ${{ needs.get-environment.outputs.major_version }}
          stability: ${{ needs.get-environment.outputs.stability }}
          release_type: ${{ needs.get-environment.outputs.release_type }}
          is_cloud: ${{ needs.get-environment.outputs.is_cloud }}
