name: widget-live-top10-memory-usage

on:
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/widget-live-top10-memory-usage.yml"
      - "widgets/centreon-widget-live-top10-memory-usage/**"
  push:
    branches:
      - dev-22.04.x
    paths:
      - ".github/workflows/centreon-widget-live-top10-memory-usage.yml"
      - "widgets/centreon-widget-live-top10-memory-usage/**"
    tags:
      - centreon-widget-live-top10-memory-usage-22.04.**

env:
  version: "22.04"

jobs:
  package:
    uses: ./.github/workflows/widget-package.yml
    with:
      widget_name: live-top10-memory-usage
    secrets:
      nexus_username: ${{ secrets.ARTI_USER }}
      nexus_password: ${{ secrets.ARTI_PASSWD }}

  delivery:
    needs: [package]
    uses: ./.github/workflows/widget-delivery.yml
    with:
      widget_name: live-top10-memory-usage
    secrets:
      nexus_username: ${{ secrets.ARTI_USER }}
      nexus_password: ${{ secrets.ARTI_PASSWD }}
