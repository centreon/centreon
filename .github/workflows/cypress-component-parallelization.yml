name: frontend-component-test

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      module_name:
        required: true
        type: string
      specs_path:
        required: true
        type: string
      spec_extension:
        required: false
        type: string
        default: ".cypress.spec.tsx"
      dependencies_lock_file:
        required: true
        type: string
        description: 'The frontend dependencies lock file path'
      is_nightly:
        required: false
        type: string
    secrets:
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
      jira_base_url:
        required: true
      jira_user_email:
        required: true
      jira_api_token:
        required: true

jobs:
  cypress-component-test-list:
    runs-on: ubuntu-24.04
    outputs:
      specs: ${{ steps.list-specs.outputs.specs }}

    steps:
      - name: Checkout sources
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - name: List of specs
        id: list-specs
        env:
          MODULE_NAME: ${{ inputs.module_name }}
          SPECS_PATH: ${{ inputs.specs_path }}
          SPEC_EXTENSION: ${{ inputs.spec_extension }}
        run: |
          cd "$MODULE_NAME"
          # SPECS_PATH is a glob pattern (e.g. "src/**", "www/**") that must be expanded by the
          # shell before being passed to find, so it is intentionally left unquoted here.
          echo "specs=$(find $SPECS_PATH -type f -name "*$SPEC_EXTENSION" -exec basename {} \; | sed -e "s/$SPEC_EXTENSION//" | sort | jq -R -s -c 'split("\n")[:-1]')" >> $GITHUB_OUTPUT

  cypress-component-test-run:
    needs: [cypress-component-test-list]
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        spec: ${{ fromJson(needs.cypress-component-test-list.outputs.specs) }}
    name: ${{ matrix.spec }}

    steps:
      - name: Checkout sources
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8
        with:
          version: 10
          run_install: false

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: ${{ inputs.dependencies_lock_file }}

      - name: Install dependencies
        run: |
          pnpm install \
            --frozen-lockfile \
            --config.trustPolicy=no-downgrade \
            --config.minimumReleaseAge=2880 \
            --config.blockExoticSubdeps=true
        working-directory: ${{ inputs.module_name }}
        shell: bash
        env:
          CYPRESS_INSTALL_BINARY: "0"
          PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD: "1"
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: "true"

      - name: Install Cypress binary
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4.0.0
        with:
          timeout_seconds: 120
          max_attempts: 10
          retry_wait_seconds: 30
          retry_on: error
          command: cd ${{ inputs.module_name }} && pnpm cypress install --force

      - name: Cypress web component testing
        uses: cypress-io/github-action@c495c3ddffba403ba11be95fffb67e25203b3799 # v7.1.10
        with:
          browser: chrome
          component: true
          install: false
          cache-key: cypress-cache-${{ runner.os }}-${{ hashFiles(inputs.dependencies_lock_file) }}
          working-directory: ${{ inputs.module_name }}
          spec: ./**/${{ matrix.spec }}${{ inputs.spec_extension }}
          publish-summary: false
          env: codeCoverageTasksRegistered=true

      - name: Clean up code coverage results
        shell: bash
        env:
          MODULE_NAME: ${{ inputs.module_name }}
        run: node centreon/packages/js-config/cypress/component/excludeNodeModulesFromCoverage.js "$MODULE_NAME/.nyc_output/out.json"

      - name: Move retries.json to reports directory and display warnings
        if: ${{ ! cancelled() }}
        env:
          MODULE_NAME: ${{ inputs.module_name }}
          SPEC: ${{ matrix.spec }}
        run: |
          retriesFile="$MODULE_NAME/cypress/results/retries.json"
          if [ -f "$retriesFile" ]; then
            jq 'to_entries | .[].key' "$retriesFile" | while read -r testName; do
              echo "::warning::Following test was retried during execution: $testName (retries: $(jq ".$testName" "$retriesFile"))"
            done
            mv "$retriesFile" "$MODULE_NAME/cypress/results/$SPEC-retries.json"
          else
            echo "No retries file found."
          fi
        shell: bash

      - name: Archive test results
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: ${{ failure() }}
        with:
          name: ${{ inputs.name }}-component-test-results-${{ matrix.spec }}
          path: |
            ${{ inputs.module_name }}/cypress/results/videos
            ${{ inputs.module_name }}/cypress/screenshots
            ${{ inputs.module_name }}/cypress/visual-testing-snapshots/**/*.diff.png
          retention-days: 1

      - name: Archive test reports
        if: ${{ ! cancelled() }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: ${{ inputs.name }}-component-test-reports-${{ matrix.spec }}
          path: ${{ inputs.module_name }}/cypress/results/*.json
          retention-days: 1

  cypress-component-test-report:
    needs: [cypress-component-test-run]
    if: |
      ! cancelled() &&
      ! contains(needs.*.result, 'cancelled')
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1

      - uses: pnpm/action-setup@0e279bb959325dab635dd2c09392533439d90093 # v6.0.8
        with:
          version: 8

      - name: Download Artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          pattern: ${{ inputs.name }}-component-test-reports-*
          path: ${{ inputs.name }}-json-reports
          merge-multiple: true

      - name: Merge reports
        env:
          NAME: ${{ inputs.name }}
        run: |
          pnpx mochawesome-merge "$NAME-json-reports/**/*-report.json" > mochawesome.json

          if ls "$NAME-json-reports/"**/*retries.json 1> /dev/null 2>&1; then
            cat "$NAME-json-reports/"**/*retries.json | jq -s add > retries.json
          else
            echo "{}" > retries.json
          fi
        shell: bash

      - name: Publish report
        if: needs.cypress-component-test-run.result == 'failure'
        uses: ./.github/actions/publish-report
        with:
          check_name: ${{ inputs.name }}-report
          path: mochawesome.json
          format: cypress

      - name: Display retries summary
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8.0.0
        with:
          script: |
            const fs = require('fs');
            const retriesFile = 'retries.json';
            const retries = JSON.parse(fs.readFileSync(retriesFile, 'utf8'));
            if (Object.keys(retries).length > 0) {
              const outputTable = [
                [{data: 'Feature', header: true}, {data: 'Scenario', header: true}, {data: 'Retries', header: true}],
              ];
              const warningTitle = `${Object.keys(retries).length} tests were retried during execution`;

              let warningBody = JSON.stringify(retries, null, 2);
              for (const testName in retries) {
                const featureName = testName.split('>')[0];
                const scenarioName = testName.split('>').slice(1).join('>');
                outputTable.push([featureName, scenarioName, String(retries[testName])]);
              }

              // Display json to allow collect of metrics
              core.warning(warningBody, { title: warningTitle });

              core.summary
                .addHeading(`Retries Summary`)
                .addTable(outputTable)
                .addDetails(`Content of ${retriesFile}`, fs.readFileSync(retriesFile, 'utf8'))
                .write();
            }

  regroup-artifacts:
    needs: [cypress-component-test-report, cypress-component-test-run]
    if: |
      ! cancelled() &&
      ! contains(needs.*.result, 'cancelled')
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        type_of_report: [test-results, test-reports]
    steps:
      - name: Merge Artifacts
        uses: actions/upload-artifact/merge@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: |
          (needs.cypress-component-test-run.result == 'failure' && contains(fromJson('["test-results", "test-reports"]'), matrix.type_of_report)) ||
          matrix.type_of_report == 'test-coverage'
        with:
          name: ${{ inputs.name }}-component-${{ matrix.type_of_report }}
          pattern: ${{ inputs.name }}-component-${{ matrix.type_of_report }}-*
          delete-merged: false # cannot be set to true due to random fails: Failed to DeleteArtifact: Unable to make request: ECONNRESET
          retention-days: 1

      - name: Delete merged artifacts
        if: |
          needs.cypress-component-test-run.result == 'success' ||
          matrix.type_of_report != 'test-coverage'
        uses: geekyeggo/delete-artifact@176a747ab7e287e3ff4787bf8a148716375ca118 # v6.0.0
        with:
          name: ${{ inputs.name }}-component-${{ matrix.type_of_report }}-*
          failOnError: false
