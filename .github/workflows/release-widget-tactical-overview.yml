---
name: Tag centreon widget tactical overview releases

on:
  pull_request_target:
    types:
      - closed
    branches:
      - "master"
      - "*.x"
      - "!dev*"
    paths:
      - ".github/actions/release/action.yml"
      - ".github/workflows/release-widget-tactical-overview.yml"
      - "widgets/centreon-widget-tactical-overview/**"
  workflow_dispatch:

env:
  module: "widget-tactical-overview"

jobs:
  release:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout sources
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Release
        id: release
        uses: ./.github/actions/release
        with:
          module_name: ${{ env.module }}
          jira_token: ${{ secrets.JIRA_TOKEN }}
