name: NPM publish
description: This publishes a package to NPM and creates a PR to update the package version

inputs:
  directory:
    description: The package directory
    required: true
  pat:
    description: The Github PAT
    required: true
  npm_token:
    description: The NPM token
    required: true
  package:
    description: The package
    required: true

runs:
  using: "composite"
  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: 17

    - name: Update JS config version
      run: |
        npm version patch -m "js-config-%s" --legacy-peer-deps
        VERSION=$(node -p "require('./package.json').version")
      working-directory: ${{ env.directory }}

    - name: Configure git
      run: |
        git config user.email "technique-ci@centreon.com"
        git config user.name "technique-ci"
      shell: bash

    - uses: JS-DevTools/npm-publish@v1
      with:
        token: ${{ inputs.npm_token }}
        package: ${{ inputs.directory }}/package.json
        tag: latest
        access: public

    - name: Create Pull Request
      uses: peter-evans/create-pull-request@v4
      with:
        token: ${{ inputs.pat }}
        committer: Technique CI <technique-ci@centreon.com>
        author: technique-ci <technique-ci@centreon.com>
        branch: MON-update-package-versions-${{ inputs.package }}-$VERSION
        title: "chore(${{ inputs.package }} version): Update package ${{ inputs.package }} version to $VERSION"
        body: |
          This updates the ${{ inputs.package }} package version to $VERSION.
          This is an automatic PR.
        labels: |
          auto-merge
        draft: false
        signoff: true
        delete-branch: true