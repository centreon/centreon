name: 'Get Latest NPM Branch Version'

description: 'Get the latest version of a package based on the branch'

inputs:
  base_code_coverage_percentage:
    description: 'The package name to get the latest version of'
    required: true

runs:
  using: 'node20'
  main: 'index.js'
