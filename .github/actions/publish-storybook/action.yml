name: publish-storybook
description: Publish storybook

runs:
  using: "composite"
  steps:
    - name: Log
      run: |
        echo ${{ steps.get_version.outputs.major_version }}
        echo ${{ steps.get_version.outputs.minor_version }}
        echo ${{ steps.get_version.outputs.release }}
      shell: bash