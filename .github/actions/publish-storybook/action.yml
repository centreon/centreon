name: publish-storybook
description: Publish storybook

inputs:
  major_version:
    description: "Major version"
    required: true

runs:
  using: "composite"
  steps:
    - name: Log
      run: |
        echo "${{ inputs.major_version }}"
      shell: bash