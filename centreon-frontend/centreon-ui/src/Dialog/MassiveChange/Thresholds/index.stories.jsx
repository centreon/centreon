import React from 'react';

import MassiveChangeThresholds from '.';

export default { title: 'Dialog/MassiveChange/Thresholds' };

export const normal = () => (
  <MassiveChangeThresholds open onCancel={() => {}} />
);
