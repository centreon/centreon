import Icon from '.';

export default { title: 'Icon/Attach' };

export const normal = (): JSX.Element => (
  <Icon
    defaultImage=""
    imgSource=""
    title="Attach"
    uploadedImage=""
    onClick={(): void => undefined}
  />
);
