name: centreon-open-tickets
arch: "${ARCH}"
platform: "linux"
version_schema: "none"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
section: "default"
priority: "optional"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Move Centreon Awie packaging to nfpm
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://www.centreon.com"
license: "Apache-2.0"

contents:
  # - src: "../www/modules/centreon-awie/*"
  #   dst: "/usr/share/centreon/www/modules/centreon-awie"
  #   file_info:
  #     mode: 0664
  #     owner: "@GROUP_OWNER@"
  #     group: "@GROUP_OWNER@"


rpm:
  summary: centreon open tickets packaging to nfpm
  signature:
    key_file: ${RPM_SIGNING_KEY_FILE}
    key_id: ${RPM_SIGNING_KEY_ID}
