name: "centreon-dsm-server"
arch: "${ARCH}"
platform: "linux"
version_schema: "none"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
section: "default"
priority: "optional"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Centreon-dsm server package
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://github.com/centreon/centreon-dsm"
license: "Apache-2.0"

contents:
  - dst: "www/modules/centreon-dsm/CHANGELOG"
    type: dir

  - dst: "/usr/share/centreon/bin/dsmd.pl"
    type: dir
    file_info:
      owner: centreon
      group: centreon
      mode: 0775

  - dst: "/usr/lib/systemd/system/dsmd.service"
    type: dir
    file_info:
      mode: 0775

  - dst: "/etc/sysconfig/dsmd"
    type: dir
    file_info:
      mode: 0775

  - src: "../README"
    dst: "/usr/share/centreon/www/modules/centreon-dsm/README"
    file_info:
      owner: "@APACHE_USER@"
      group: "@APACHE_GROUP@"

  - src: "../www/modules/centreon-dsm"
    dst: "/usr/share/centreon/www/modules/centreon-dsm"
    file_info:
      owner: "@APACHE_USER@"
      group: "@APACHE_GROUP@"

  - src: "../cron/centreon-dsm"
    dst: "/etc/cron.d/centreon-dsm"

scripts:
  postinstall: ./scripts/centreon-dsm-server-postinstall.sh

overrides:
  rpm:
    depends:
      - "/sbin/chkconfig"
      - centreon-web >= ${MAJOR_VERSION}
      - centreon-web < ${NEXT_MAJOR_VERSION}
  deb:
    depends:
      - "centreon-web (>= ${MAJOR_VERSION}~)"
      - "centreon-web (<< ${NEXT_MAJOR_VERSION}~)"

rpm:
  summary: Centreon-dsm server
  signature:
    key_file: ${RPM_SIGNING_KEY_FILE}
    key_id: ${RPM_SIGNING_KEY_ID}
