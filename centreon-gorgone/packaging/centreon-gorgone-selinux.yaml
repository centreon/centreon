name: "centreon-gorgone"
arch: "${ARCH}"
platform: "linux"
version_schema: "none"
version: "${VERSION}"
release: "${DIST}"
section: "default"
priority: "optional"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Selinux for centreon-gorgone
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://www.centreon.com"
license: "Apache-2.0"

contents:
  - src: "centreon-gorgone/selinux/centreon-gorgoned.pp"
    dst: "/usr/share/selinux/packages/centreon/centreon-gorgoned.pp"
    file_info:
      mode: 0655

scripts:
  postinstall: ./scripts/centreon-gorgone-selinux-postinstall.sh
  preremove: ./scripts/centreon-broker-selinux-preremove.sh

rpm:
  summary: selinux for centreon-gorgone
  signature:
    key_file: ${RPM_SIGNING_KEY_FILE}
    key_id: ${RPM_SIGNING_KEY_ID}
