<?php

$code = 1;
$msg = "None";

$resultat = array(
    "code" => $code,
    "msg" => $msg,
    "result" => "plop"
);

# $path = "../";
# $tpl = new Smarty();
# $tpl = initSmartyTplForPopup($path, $tpl, 'templates', $centreon_path);
# $tpl->assign('rules', $rules);
    
#  $tpl->fetch("rules.ihtml")

?>