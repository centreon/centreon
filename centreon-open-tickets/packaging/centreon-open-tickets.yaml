name: centreon-open-tickets
arch: "${ARCH}"
platform: "linux"
version_schema: "none"
version: "${VERSION}"
release: "${RELEASE}${DIST}"
section: "default"
priority: "optional"
maintainer: "Centreon <contact@centreon.com>"
description: |
  Move Centreon Awie packaging to nfpm
  Commit: @COMMIT_HASH@
vendor: "Centreon"
homepage: "https://www.centreon.com"
license: "Apache-2.0"

contents:
  - src: "../www/modules/centreon-open-tickets"
    dst: "/usr/share/centreon/www/modules/centreon-open-tickets"
    file_info:
      owner: "@APACHE_OWNER@"
      group: "@APACHE_OWNER@"

  - src: "../widgets/open-tickets"
    dst: "/usr/share/centreon/www/widgets/centreon-open-tickets"
    file_info:
      owner: "@APACHE_OWNER@"
      group: "@APACHE_OWNER@"

rpm:
  summary: centreon open tickets packaging to nfpm
  signature:
    key_file: ${RPM_SIGNING_KEY_FILE}
    key_id: ${RPM_SIGNING_KEY_ID}
